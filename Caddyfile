# Plain-HTTP reverse proxy to api-gateway, for deploying by bare IP with no domain yet.
# Once a domain is pointed at the server, replace ":80" with the domain name so Caddy
# provisions Let's Encrypt TLS automatically, then re-run `docker compose up -d caddy`.
:80 {
	reverse_proxy api-gateway:8080
}
